# Example Use Case

Prompt:

```text
Show me the minimum files required for a free-pack submission.
```

Expected direction:

```text
Create packs/<creator>/<slug>/ with manifest.json and SKILL.md, then add optional supporting files only if they are needed.
```
